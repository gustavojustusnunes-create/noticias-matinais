name: Video Diario (Email)

on:
  schedule:
    # Roda todos os dias às 10:00 UTC (07:00 BRT, logo após o email diário das 06:00 BRT)
    - cron: '0 10 * * *'
  workflow_dispatch: # Permite disparo manual via botão no GitHub

jobs:
  gerar_e_enviar_video:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
    - name: Checkout Repo
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.13' # Mesma versão do repo

    - name: Cache pip
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt', '**/requirements-video.txt') }}

    - name: Install System Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg fonts-dejavu

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-video.txt

    - name: Generate and Email Video
      env:
        CLAUDE_KEY: ${{ secrets.GEMINI_API_KEY }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        EMAIL_USER: ${{ secrets.SMTP_USER }}
        EMAIL_PASSWORD: ${{ secrets.SMTP_PASS }}
        EMAIL_FROM: ${{ secrets.FROM_EMAIL }}
        SMTP_HOST: ${{ secrets.SMTP_HOST }}
        SMTP_PORT: ${{ secrets.SMTP_PORT }}
      run: |
        python enviar_video_diario.py

    - name: Salvar Vídeo como Arquivo Baixável (Backup)
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: Video-Diario
        path: assets/videos_gerados/*.mp4
        retention-days: 7
