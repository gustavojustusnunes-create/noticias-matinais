name: Despertador do Site

on:
  schedule:
    - cron: '0 */2 * * *' # Roda a cada 2 horas
  workflow_dispatch: # Botão manual

jobs:
  wake_up:
    runs-on: ubuntu-latest
    steps:
    - name: Ping no Site
      run: |
        # Substitua pelo SEU link do Streamlit
        curl -I https://noticias-matinais-w3utkdwxerflwqytjyc7p2.streamlit.app/
